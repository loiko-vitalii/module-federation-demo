name: deploy-app

on:
  push:
    branches:
      - main
    paths:
      - 'app/**'
  workflow_dispatch:

jobs:
  tests:
    uses: ./.github/workflows/playwright.yml
    secrets: inherit

  build:
    needs: tests

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Build with Docker
        run: |
          docker build \
            --target builder \
            --build-arg PRODUCTION_DOMAIN=${{ secrets.PRODUCTION_DOMAIN }} \
            -f app/Dockerfile \
            -t app-build .

      - name: Extract build artifacts
        run: |
          docker create --name temp-container app-build
          docker cp temp-container:/app/app/dist ./dist
          docker rm temp-container
        
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to AWS S3
        run: |
          VERSION=$(docker run --rm uikit-build node -p "require('./package.json').version")
          aws s3 sync dist s3://${{ secrets.AWS_BUCKET_NAME }}/app/${VERSION} --delete
          aws s3 sync dist s3://${{ secrets.AWS_BUCKET_NAME }}/app/latest --delete


      - name: Invalidate CloudFront Distribution
        run: |
          aws cloudfront create-invalidation --distribution-id ${{ secrets.DISTRIBUTION_ID }} --paths "/app/latest/*"
